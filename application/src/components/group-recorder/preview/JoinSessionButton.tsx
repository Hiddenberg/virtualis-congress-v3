// "use client"

// import { useGroupRecorderContext } from "@/contexts/GroupRecorderContext";
// import { useState } from "react";

// function JoinSessionButton () {
//    const [isLoading, setIsLoading] = useState(false);
//    const { joinSession } = useGroupRecorderContext();

//    return (
//       <button onClick={() => {setIsLoading(true); joinSession()}}
//          disabled={isLoading}
//          className="block bg-black mx-auto p-4 rounded-xl font-semibold text-white disabled:animate-pulse">
//          {isLoading ? "Uniendote a la sesión..." : "Unirse a la sesión"}
//       </button>
//    );
// }

// export default JoinSessionButton;
